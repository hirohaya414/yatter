<div class="row justify-content-center">
  <div class="col-8">
    <span class="follow-underline">フォロワー(全<%= @members_all.count %>件)</span>
    <%= render '/members/index', members: @members %>
    <%= paginate @members %>

    <div class="text-center actions">
      <%= link_to '戻る', 'javascript:history.back()' %>
    </div>
  </div>
</div>
